# 阶段 34：建设批次、机组编号和证据日期

本阶段不运行省级求解器，不改阶段 23 原库存。`reviewed_events.json` 是人工审读事件表；`unit_evidence_overlay.csv` 保留全部 109 条原字段，仅叠加项目/容量组层面的候选关联。关联不等于唯一设备身份确认。

```bash
python3 work/research/analysis/audit_ccgt_primary.py --output-dir work/tmp/ccgt-primary-replay
```

解释器仅需标准库。按 `source_access_log.json` 恢复三个 `raw_html` 原件至记录的路径并核对 SHA-256；动态网页日后改变须重新审读，不能只改预期哈希。戚墅堰网页声明 GBK，另外两个为 UTF-8；禁止带替换字符的容错解码。解析不执行脚本。

17 项检查覆盖三个原件哈希、关键容量/日期、无锡表格行与储备项目分段、源库存哈希及全量字段/容量保留。它们不自动证明人工语义判断。109 条原记录仍为候选；15 条 CCGT 的 CHP 未知不改成无供热；0 套运行参数准入。

南通证据只取得官方网页的工具提取正文或检索文本，未取得可冻结的 HTML 原件。中国能建 2026-07-23 文章已通过 web open 阅读完整正文；业主动态列表及施工方报道提供旁证，但同一事件不能当作多个独立运行样本。原网站直接下载出现证书/连接错误或 HTTP 418，保持证书验证；访问失败全部保留。复跑本脚本只检查人工事件表结构及引用，不冒充重新联网验证这些报道。

下一步优先补：南通实际设备编号与 GEM ID 的唯一对应；无锡初建与规划扩建之间的项目许可关联；戚墅堰不同容量组当前的供热义务。未明确源时间点前，不将历史网页描述推广至 2030 年。应同时排查相近项目名称是否指向同一资产，例如库存两组 Funing 记录是待核实线索，尚未认定重复或删除容量。之后才能结合供热、燃料、电表、启停与可用率形成运行输入。

阶段 34 审计已在另一输出目录复跑，两份派生产物逐字节一致，见 `ccgt_primary/replay_comparison.json`（该目录内为 `replay_comparison.json`）。这仅为同机重建验证。
