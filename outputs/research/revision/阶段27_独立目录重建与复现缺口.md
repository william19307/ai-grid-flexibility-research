# 阶段 27：独立目录重建与复现缺口

2026-09-23。本轮从网络克隆修订仓库，建立隔离环境并恢复一段真实输入链。结论：13/14 项必需输入与冻结哈希一致，267 项数学检查通过；甘肃电价原始页面尚不能恢复成冻结版本，阶段 24/26 的认证轨迹复算没有执行。整篇论文复现门槛仍未关闭。

## 实际完成的部分

初始新副本为 `3ffe061`，Git 状态干净；目标列表中的两份派生 CSV 已随 Git 取得，其余 12 项未存在。未从原工作树复制 sources、prepared 或运行环境。手工年度转录表本来就受 Git 跟踪，明确作为既有转录使用，没有重新声称独立核对统计年鉴图像。

同一台 Mac 上新建 Python 3.14 隔离环境，从公共 PyPI 使用 `--no-cache-dir` 安装固定版本 numpy、pandas、scipy、h5py 等依赖；user site 关闭，依赖检查通过。它不是另一台机器或另一操作系统的复现。

从[Helios 作者仓库](https://github.com/S-Lab-System-Group/HeliosData)的固定提交重新下载 36,437,672 字节压缩包，整包和八个 CSV 的原哈希全部一致。江苏电价 PDF、贵州电价 HTML 也重新取得并与原哈希一致。最初的江苏探测曾返回 403，正式恢复请求成功，保留请求结果的区别，不将瞬时失败当作永久不可得。

从 PyPSA-China 固定版本压缩包读取负荷 HDF5 成员，检查范围响应、成员名、长度、CRC32 和 SHA256；没有下载整个 16.9 GB 包，也不声称整包已验证。使用下载的成员及 Git 转录表重建 31 省、8,784 小时负荷数组。

## 找到并修复的计算顺序问题

首次候选数组哈希不符。独立逐字段比较表明：时钟、省名、年度目标与状态一致；负荷最大差为 4.22×10⁻¹⁰ MW，29 个缩放因子有至多 6.22×10⁻¹⁵ 的差。新 DataFrame 返回 Fortran 顺序数组，原冻结数组是 C 顺序，列求和累加路径不同。

提交 `a8c2003` 显式保持原 C 顺序，新副本通过网络拉取修正后，只用已经从网络恢复且通过哈希的 HDF5 和转录表重建。所得 NPZ 精确匹配原 `86bc5a2a…` 哈希。没有修改目标哈希、调节负荷或改动原始参考数组。原研究树中的 NPZ 仅用于诊断比较，没有复制进新副本。初次失败和修正后记录都保留。

## 仍未通过的来源门槛

甘肃登记表只写了政府原页和“使用 gsei 镜像”，没有保存确切历史镜像 URL。根据原 HTML 条目编号构造的动态入口返回 403；随后通过标题检索和原页面栏目/日期定位到两个公开文章入口：

- [公开动态文章入口](https://manage.gsei.com.cn/index.php/cms/item-view-id-303607-page-1)：21,576 字节，哈希 `eede38f8…`。
- [公开静态文章入口](https://www.gsei.com.cn/html/1668/2020-12-02/content-303607.html)：19,576 字节，哈希 `66d30a4a…`。

原冻结文件为 21,663 字节、`63be4112…`。两个新页面均不匹配，不能自动替代；“文章主题相同”不等于原件逐字节恢复。没有绕过登录或访问控制。新入口已写入恢复清单，但预期原哈希保持不变，所以未来拿到相同新版页面仍会被拒绝。

需要取得原历史快照，或对新来源做独立内容审读并建立明确的新证据/认证版本。单纯把旧哈希改成新页面哈希不构成复现，也不能重签旧结果来强行通过。本轮没有这么做。

## 新环境中的验证边界

机组启停 75 项、电网 118 项、水库 74 项，共 267 项检查通过。包含相应独立枚举、容量/任务分配和动态规划参照。它们是数学检查，不能替代缺失来源或证明真实物理校准。

因完整输入门槛仍为 13/14，认证轨迹的阶段 24/26 复算未启动，既有结果也未覆盖。新副本最终 Git 跟踪文件保持干净，无新后台作业；主研究目录、原已投稿稿件、旧原始输入和天气暂停状态保持。

完整命令见 `clean_reconstruction/RUNBOOK.md`；首次恢复、数组诊断、修正后准入、公开入口结果、隔离环境/检查和汇总均保存在该目录。下一步继续补全来源恢复，并落实尚缺的物理输入与全年验证，不能把本轮报告当作完整 Nature Energy 质量认证。
